# AGENTS.md

This repo's canonical steering lives in **[CLAUDE.md](./CLAUDE.md)** — read that.

We're a first-class Claude Code repo: `CLAUDE.md` is the single source of truth at every altitude (root and per-module). This pointer redirects tools that look for `AGENTS.md`.
